<% format ||= "xlsx" %>

<%= form_tag("/los/download." + format, :method=>'post', :multipart => true) do %>
	<%= hidden_field_tag(:lo_ids, getLoIds(@los)) %>
	<%= submit_tag(t("actions.download")) %>
<% end %>
|
<%= form_tag("/los/downloadevs." + format, :method=>'post', :multipart => true) do %>
	<%= hidden_field_tag(:lo_ids, getLoIds(@los)) %>
	<%= submit_tag(t("actions.download_evaluations")) %>
<% end %>