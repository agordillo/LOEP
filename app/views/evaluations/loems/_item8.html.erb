<!-- The individual checkbox information is stored in the following slots: item46-item48 -->
<%item8checkbox1Value = @evaluation.send("item46")%>
<%item8checkbox2Value = @evaluation.send("item47")%>
<%item8checkbox3Value = @evaluation.send("item48")%>

<%= f.hidden_field "item46", :value => item8checkbox1Value, :class => "skipValidation" %>
<%= f.hidden_field "item47", :value => item8checkbox2Value, :class => "skipValidation" %>
<%= f.hidden_field "item48", :value => item8checkbox3Value, :class => "skipValidation" %>

<p class="input_wrappers_header"> The feedback is:</p>
<p class="input_wrapper">
  <input nItem="46" type="checkbox" class="skipValidation" <%= item8checkbox1Value==1 ? "checked='checked'" : "" %> <%= readonly ? "disabled=\"disabled\"" : ""%>></input>
  <span>an appropriate amount for the user (not too much or too little).</span>
</p>
<p class="input_wrapper">
  <input nItem="47" type="checkbox" class="skipValidation" <%= item8checkbox2Value==1 ? "checked='checked'" : "" %> <%= readonly ? "disabled=\"disabled\"" : ""%>></input>
  <span>well written with appropriate language for the user.</span>
</p>
<p class="input_wrapper">
  <input nItem="48" type="checkbox" class="skipValidation" <%= item8checkbox3Value==1 ? "checked='checked'" : "" %> <%= readonly ? "disabled=\"disabled\"" : ""%>></input>
  <span>appears when necessary and gives user adequate direction and information for progressing.</span>
</p>

<% if !readonly %>
  <script type="text/javascript">
    var getItem8Score = function(itemsChecked){
      switch(itemsChecked){
          case 3:
              return 3;
          case 2:
              return 2;
          default:
              return 1;
      };
    };

    $(document).ready(function(){
      loadEventsForComplexItem("8");
    });
  </script>
<% end %>