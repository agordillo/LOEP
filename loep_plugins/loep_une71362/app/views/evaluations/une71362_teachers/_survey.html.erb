<% embed ||= false %>
<% formOptions ||= @evaluation.getFormOptions %>
<% scale ||= @evaluation.getScale %>

<%= render 'evaluations/une71362/survey', :f => f, :readonly => readonly, :embed => embed, :formOptions => formOptions, :scale => scale, :entity => "teacher", :sections => Evaluations::Une71362Teacher.getSections %>